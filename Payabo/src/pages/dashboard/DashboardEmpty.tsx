import { PlaceholderPage } from "../PlaceholderPage";

export const DashboardEmpty = () => {
  return <PlaceholderPage title="Dashboard (Empty)" description="This layout mirrors the empty dashboard state." />;
};
