import { AiModelsPage } from '@/pages/ai/AiModelsPage';
import type { WorkspacePanelRenderProps } from '../types';

export function AiModelsPanel(_: WorkspacePanelRenderProps) {
  return <AiModelsPage />;
}
