import { CommercePlaceholder } from './CommercePlaceholder';

export function DeliveryCalendarPage() {
  return (
    <CommercePlaceholder
      title="Delivery"
      subtitle="The fulfilment calendar and the promise it computes right now"
      spec="077"
      summary="Delivery days, cutoff, lead time and blackout dates on a month grid — with the live earliest-delivery promise echoed on every save."
    />
  );
}
