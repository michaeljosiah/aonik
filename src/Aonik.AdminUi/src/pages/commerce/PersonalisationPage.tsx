import { CommercePlaceholder } from './CommercePlaceholder';

export function PersonalisationPage() {
  return (
    <CommercePlaceholder
      title="Personalisation"
      subtitle="The tenant option catalogue and how each product narrows it"
      spec="074"
      summary="Option groups and choices with absolute prices, recommended defaults (with affected-product impact), and the per-product narrowing editor that preserves inherit-vs-pinned choice lists."
    />
  );
}
