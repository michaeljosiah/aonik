import { CommercePlaceholder } from './CommercePlaceholder';

export function CommerceOrdersPage() {
  return (
    <CommercePlaceholder
      title="Storefront orders"
      subtitle="Checked-out storefront orders with payment, fulfilment and preparation detail"
      spec="083"
      summary="The storefront order list (buyer, payment status, box size) and the full order drawer: aggregate vs add-on lines, the kitchen landing and the charge envelope."
    />
  );
}
