import { CommercePlaceholder } from './CommercePlaceholder';

export function BoxPlansPage() {
  return (
    <CommercePlaceholder
      title="Box plans"
      subtitle="Size-tiered container pricing — formula, presets and savings"
      spec="076"
      summary="Per-bundle size plans: the base-plus-per-space formula, preset overrides with authored savings, and the price curve showing exactly what each size charges."
    />
  );
}
