import { CommercePlaceholder } from './CommercePlaceholder';

export function CommerceOverviewPage() {
  return (
    <CommercePlaceholder
      title="Commerce overview"
      subtitle="Attention-first dashboard for the storefront operation"
      spec="084"
      summary="KPIs, needs-attention queues (content review, unpriceable extras, calendar promise) and shortcuts into every commerce surface."
    />
  );
}
