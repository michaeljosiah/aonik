import { CommercePlaceholder } from './CommercePlaceholder';

export function ProductContentPage() {
  return (
    <CommercePlaceholder
      title="Product content"
      subtitle="Option-dependent declarations — authored exactly or withheld, never substituted"
      spec="075"
      summary="Tenant content-status board (stale and review queues), the default-block editor, per-combination variants and single-choice coverage gaps."
    />
  );
}
