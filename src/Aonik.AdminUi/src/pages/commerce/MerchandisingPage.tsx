import { CommercePlaceholder } from './CommercePlaceholder';

export function MerchandisingPage() {
  return (
    <CommercePlaceholder
      title="Merchandising"
      subtitle="Collections, filter facets and the category tree"
      spec="078"
      summary="Curated collections with rank editing (extras rail pricing state included), configurable facet groups, and category management."
    />
  );
}
