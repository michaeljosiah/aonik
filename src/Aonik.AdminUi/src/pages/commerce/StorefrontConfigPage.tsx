import { CommercePlaceholder } from './CommercePlaceholder';

export function StorefrontConfigPage() {
  return (
    <CommercePlaceholder
      title="Storefront config"
      subtitle="The tunables document a storefront must never hard-code"
      spec="079"
      summary="Currency labelling, recommended-choice wording, delivery display amounts, default box and extras collection — edited against a live preview of the public document."
    />
  );
}
