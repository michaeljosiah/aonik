<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <RootNamespace>Aonik.Finance.Mcp</RootNamespace>
    <!-- Development-only host: registers blanket-trust security stubs (grant-all
         IPermissionService, auto-clear IComplianceService) guarded at startup by
         DevelopmentOnlyHostGuard. Never deploy / publish / containerize. See finding C4. -->
    <IsPublishable>false</IsPublishable>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\Aonik.Finance\Aonik.Finance.csproj" />
    <!-- Direct reference (Spec 027 S5): the FinancialLifeGraph MCP tools consume
         PersonalFinance's IFinancialLifeGraphService + FLG response models, which
         previously resolved transitively through Aonik.Finance. That edge was
         dropped, so the (already-compiled) dependency is now made explicit. -->
    <ProjectReference Include="..\Aonik.PersonalFinance\Aonik.PersonalFinance.csproj" />
    <ProjectReference Include="..\Aonik.Ai\Aonik.Ai.csproj" />
    <ProjectReference Include="..\Aonik.Agents\Aonik.Agents.csproj" />
    <ProjectReference Include="..\Aonik.SharedKernel\Aonik.SharedKernel.csproj" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="ModelContextProtocol" Version="1.4.0" />
    <PackageReference Include="Microsoft.Extensions.Hosting" Version="10.0.3" />
  </ItemGroup>

</Project>
