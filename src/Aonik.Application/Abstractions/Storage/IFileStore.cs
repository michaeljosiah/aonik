// The canonical IFileStore and FileUploadResult definitions live in
// Aonik.SharedKernel.Abstractions.Storage so that module projects
// (Finance, Platform, etc.) can depend on them without referencing Application.
//
// This file is intentionally empty; imports should use:
//   using Aonik.SharedKernel.Abstractions.Storage;
