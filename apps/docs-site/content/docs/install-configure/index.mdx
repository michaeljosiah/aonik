---
title: Install & Configure
description: Every supported way to install Aonik, the connection-string knobs, and the bootstrap flow.
---

<Callout type="warn" title="Coming soon">
  This section is being written in a later phase of the docs rewrite. Until then, the [Quickstart](/docs/getting-started/quickstart) covers the recommended local path.
</Callout>

## What this section will cover

- Run with Aspire (recommended)
- Run with Docker Compose
- Run without Docker (LocalDB + `dotnet run`)
- Environment variables reference (every `Database__*`, `Qdrant__*`, `Auth__*`, etc.)
- Connect to SQL Server (LocalDB, Azure SQL)
- Connect to Qdrant (local, cloud, multi-tenant prefixes)
- Bootstrap your first tenant + admin

## In the meantime

- [Quickstart](/docs/getting-started/quickstart) — the recommended local-dev install path
- `apps/docs-site/docs.old/deployment/` (in the repo) — pre-rewrite deployment guides
